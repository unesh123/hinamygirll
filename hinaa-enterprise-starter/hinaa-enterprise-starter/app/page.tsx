"use client";
import {useState} from 'react';import Shell from '@/components/Shell';import {Send} from 'lucide-react';
type M={role:'user'|'assistant';content:string};
export default function Chat(){const [messages,setMessages]=useState<M[]>([{role:'assistant',content:'Hey Unesh ✨ I am HINAA. What should we build today?'}]);const [input,setInput]=useState('');const [busy,setBusy]=useState(false);
async function send(){const text=input.trim();if(!text||busy)return;const next=[...messages,{role:'user' as const,content:text}];setMessages(next);setInput('');setBusy(true);setMessages([...next,{role:'assistant',content:''}]);try{const r=await fetch('/api/chat',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({messages:next})});if(!r.ok)throw new Error(await r.text());const rd=r.body!.getReader(),dec=new TextDecoder();let acc='';while(true){const {done,value}=await rd.read();if(done)break;acc+=dec.decode(value,{stream:true});setMessages([...next,{role:'assistant',content:acc}]);}}catch(e){setMessages([...next,{role:'assistant',content:'HINAA could not complete that request. '+(e as Error).message}]);}finally{setBusy(false)}}
return <Shell title="Chat with HINAA"><section className="chat"><div className="messages">{messages.map((m,i)=><div key={i} className={'bubble '+m.role}>{m.content||'Thinking…'}</div>)}</div><div className="composer"><textarea value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}} placeholder="Ask HINAA anything…"/><button className="send" onClick={send}><Send size={18}/></button></div></section></Shell>}