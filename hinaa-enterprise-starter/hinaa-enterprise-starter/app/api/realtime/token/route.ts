export async function POST(){const key=process.env.OPENAI_API_KEY;if(!key)return Response.json({error:'OPENAI_API_KEY missing'},{status:500});const r=await fetch('https://api.openai.com/v1/realtime/client_secrets',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({session:{type:'realtime',model:process.env.OPENAI_REALTIME_MODEL||'gpt-realtime',audio:{output:{voice:'marin'}}}})});const data=await r.json();return Response.json(r.ok?{value:data.value}:{error:data.error?.message||'Token creation failed'},{status:r.status})}